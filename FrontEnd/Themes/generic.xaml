<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:FrontEnd.Forms"
    xmlns:report="clr-namespace:FrontEnd.Reports"
    xmlns:formComp="clr-namespace:FrontEnd.Forms.FormComponents"
    xmlns:ctr="clr-namespace:FrontEnd.Forms.Controls" 
    xmlns:fr="clr-namespace:FrontEnd.Forms"
    xmlns:theme="clr-namespace:Microsoft.Windows.Themes;assembly=PresentationFramework.AeroLite">
    
    <!--Forms-->
    <Style TargetType="{x:Type local:Form}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:Form}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="{TemplateBinding MenuRow}"/>
                            <RowDefinition Height="{TemplateBinding HeaderRow}"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="5"/>
                            <RowDefinition Height="{TemplateBinding RecordTrackerRow}"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="{TemplateBinding RecordStatusColumn}"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <ContentPresenter Grid.Row="0" Grid.ColumnSpan="2" Content="{TemplateBinding Menu}"/>
                        <ContentPresenter Grid.Row="1" Grid.ColumnSpan="2" Content="{TemplateBinding Header}"/>
                        <ContentPresenter Grid.Row="2" Grid.Column="1" Content="{TemplateBinding Content}"
                                          ContentTemplate="{TemplateBinding ContentTemplate}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        <formComp:RecordStatus Grid.Row="2" Controller="{TemplateBinding Controller}"/>
                        <ProgressBar Grid.Row="3" Grid.ColumnSpan="2" IsIndeterminate="{TemplateBinding IsLoading}"/>
                        <formComp:RecordTracker Grid.Row="4" Grid.ColumnSpan="2" Controller="{TemplateBinding Controller}"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--FormRow-->
    <Style TargetType="{x:Type local:FormRow}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:FormRow}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="{TemplateBinding RecordStatusColumn}" MaxWidth="23"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <ContentPresenter Grid.Column="1" Content="{TemplateBinding Content}"
                                          ContentTemplate="{TemplateBinding ContentTemplate}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        
                        <formComp:RecordStatus IsDirty="{TemplateBinding IsDirty}" VerticalAlignment="Top" VerticalContentAlignment="Top" Height="30"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!--FormLists-->
    <Style TargetType="{x:Type local:FormList}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:FormList}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="{TemplateBinding MenuRow}"/>
                            <RowDefinition Height="{TemplateBinding HeaderRow}"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="5"/>
                            <RowDefinition Height="{TemplateBinding RecordTrackerRow}"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <ContentPresenter Grid.Row="0" Content="{TemplateBinding Menu}"/>
                        <ContentPresenter Grid.Row="1" Content="{TemplateBinding Header}"/>

                        <ContentPresenter x:Name="Lista" Grid.Row="2" Content="{TemplateBinding Content}"
                                          ContentTemplate="{TemplateBinding ContentTemplate}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        
                        <ProgressBar Grid.Row="3" IsIndeterminate="{TemplateBinding IsLoading}"/>
                        <formComp:RecordTracker Grid.Row="4" Controller="{TemplateBinding Controller}"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--SubForm-->
    <Style TargetType="{x:Type local:SubForm}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:SubForm}">
                    <Border BorderBrush="Black" BorderThickness="1" CornerRadius="5">
                        <fr:FormPresenter
                                          Content="{TemplateBinding Content}"
                                          ContentTemplate="{TemplateBinding ContentTemplate}"
                                          Controller="{TemplateBinding Controller}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--Loading Mask-->
    <Style TargetType="{x:Type ctr:LoadingMask}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ctr:LoadingMask}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="20"/>
                            <RowDefinition Height="25"/>
                        </Grid.RowDefinitions>
                        <ContentPresenter Content="{TemplateBinding Content}"
                                          ContentTemplate="{TemplateBinding ContentTemplate}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        <ProgressBar Grid.Row="1" IsIndeterminate="True"/>
                        <Label HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="2" Content="Developed by Salvatore Amaddio R."/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--RecordTracker-->
    <Style TargetType="{x:Type formComp:RecordTracker}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type formComp:RecordTracker}">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Stretch">
                        <StackPanel.Background>
                            <SolidColorBrush Color="{x:Static SystemColors.MenuColor}" />
                        </StackPanel.Background>
                        <StackPanel.Resources>
                            <Style TargetType="Button">
                                <Setter Property="Width" Value="33"/>
                                <Setter Property="Command" Value="{Binding OnClickCommand, RelativeSource={RelativeSource TemplatedParent}}"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="FontSize" Value="20"/>
                            </Style>
                        </StackPanel.Resources>
                        <Button Content="🢐🢐" ToolTip="First Record" CommandParameter="1"/>
                        <Button Content="🢐" ToolTip="Previous Record" CommandParameter="2"/>
                        <Label Content="{TemplateBinding Records}" VerticalAlignment="Center" VerticalContentAlignment="Center" Background="White"/>
                        <Button Content="🢒" ToolTip="Next Record" CommandParameter="3"/>
                        <Button Content="🢒🢒" ToolTip="Last Record" CommandParameter="4"/>
                        <Separator Width="20" Background="Transparent"/>
                        <Button Content="NEW" FontSize="10" ToolTip="New Record" Visibility="{TemplateBinding GoNewVisibility}" CommandParameter="5"/>
                    </StackPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--RecordStatus-->
    <Style TargetType="{x:Type formComp:RecordStatus}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type formComp:RecordStatus}">
                    <Grid VerticalAlignment="Stretch">
                        <Grid.Resources>
                            <formComp:IsDirtyConverter x:Key="IsDirtyConverter"/>
                        </Grid.Resources>
                        <Grid.Background>
                            <SolidColorBrush Color="{x:Static SystemColors.MenuColor}" />
                        </Grid.Background>
                        <Label Padding="5,0,0,0" Content="{Binding IsDirty, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource IsDirtyConverter}}" VerticalAlignment="Center" VerticalContentAlignment="Center" HorizontalContentAlignment="Left" HorizontalAlignment="Left" FontWeight="Bold" FontSize="20"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--CommandPanel-->
    <Style TargetType="{x:Type local:CommandPanel}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:CommandPanel}">
                    <Grid>
                        <Label Content="COMMANDS" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,0,0,0" FontWeight="Bold" FontStyle="Italic" Background="White" Padding="5,5,5,0" Panel.ZIndex="1"/>
                        <Border BorderThickness="1" BorderBrush="Black" Padding="10" VerticalAlignment="Center" CornerRadius="5">
                            <StackPanel>
                                <StackPanel.Resources>
                                    <Style TargetType="Button" x:Key="style">
                                        <Setter Property="MaxWidth" Value="100"/>
                                        <Setter Property="MaxHeight" Value="30"/>
                                        <Setter Property="CommandParameter" Value="{Binding CommandParameter, RelativeSource={RelativeSource TemplatedParent}}"/>
                                    </Style>
                                </StackPanel.Resources>
                                <local:SaveButton Style="{DynamicResource style}" Command="{TemplateBinding UpdateCMD}"/>
                                <Separator Height="1" Background="Transparent"/>
                                <local:DeleteButton Style="{DynamicResource style}" Command="{TemplateBinding DeleteCMD}"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--FilterOption-->
    <Style TargetType="{x:Type local:FilterOption}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:FilterOption}">
                    <Grid Grid.Column="1">
                        <Grid.Resources>
                            <SolidColorBrush Color="{x:Static SystemColors.MenuColor}" x:Key="DefColor"/>
                        </Grid.Resources>
                        <Label x:Name="PART_label" Content="{TemplateBinding Text}"/>
                        <Button x:Name="PART_dropdown_button" VerticalContentAlignment="Top" HorizontalAlignment="Right" Background="Transparent" BorderThickness="0" Padding="0" VerticalAlignment="Bottom" Margin="0,0,1.5,4" Height="10" ToolTip="{TemplateBinding ToolTip}"/>
                        <Popup x:Name="PART_popup" IsOpen="{TemplateBinding IsOpen}" Placement="Bottom" PlacementTarget="{Binding ElementName=PART_label}" PopupAnimation="Fade" AllowsTransparency="True" StaysOpen="False">
                            <Border BorderBrush="Black" BorderThickness=".5" Padding="1" Background="White">
                                <Grid Background="White">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="20"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                    <Button Content="Clear" Background="{DynamicResource DefColor}" BorderThickness="0" FontWeight="Bold" Margin="0,0,0,0" x:Name="PART_clear_button"/>
                                    <ListBox ItemsSource="{TemplateBinding ItemsSource}" BorderThickness="0" Grid.Row="1">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <Grid local:Definition.ColumnDefinitions="100,20">
                                                <TextBlock Text="{Binding Value}"/>
                                                <CheckBox Grid.Column="1" IsChecked="{Binding IsSelected}"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </Grid>
                            </Border>
                        </Popup>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--ReportPage-->
    <Style TargetType="{x:Type report:ReportPage}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type report:ReportPage}">
                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding Background}" BorderThickness="0" Padding="{TemplateBinding Padding}">
                        <Grid x:Name="Page" Background="{TemplateBinding Background}" Width="{TemplateBinding PageWidth}" Height="{TemplateBinding PageHeight}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="{TemplateBinding HeaderRow}"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="{TemplateBinding FooterRow}"/>
                            </Grid.RowDefinitions>

                            <ContentPresenter Content="{TemplateBinding Header}"/>
                            <ContentPresenter Grid.Row="1" Content="{TemplateBinding Body}"/>
                            <ContentPresenter Grid.Row="2" Content="{TemplateBinding Footer}"/>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--ReportViewer-->
    <Style TargetType="{x:Type report:ReportViewer}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type report:ReportViewer}">
                    <Grid Background="Gray">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="40"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="5"/>
                            <RowDefinition Height="30"/>
                        </Grid.RowDefinitions>

                        <ToolBarTray Grid.Row="0" IsLocked="True">
                            <ToolBarTray.Background>
                                <SolidColorBrush Color="{x:Static SystemColors.MenuColor}" />
                            </ToolBarTray.Background>
                            <ToolBar IsOverflowOpen="False">
                                <ToolBar.Background>
                                    <SolidColorBrush Color="{x:Static SystemColors.MenuColor}" />
                                </ToolBar.Background>
                                <report:PDFButton Width="30" Background="Transparent" BorderThickness="0" VerticalAlignment="Center" Command="{TemplateBinding PrintCommand}"/>
                                <Separator Width="10" Background="Transparent"/>
                                <fr:Text x:Name="text_fileName" Placeholder="File name..." Text="{Binding Path=FileName, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="200" VerticalAlignment="Center"/>
                                <Separator Width="10" Background="Transparent"/>
                                <StackPanel VerticalAlignment="Center">
                                    <CheckBox IsChecked="{Binding Path=OpenFile, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Content="Open File after printing" Width="160" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </StackPanel>
                            </ToolBar>
                        </ToolBarTray>
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                            <Border x:Name="Border" HorizontalAlignment="Center" Margin="0,0,0,10" BorderThickness="0" BorderBrush="Black" ScrollViewer.CanContentScroll="True">
                                <report:ListPage x:Name="ListPage" ItemsSource="{Binding Path = ItemsSource, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}" SelectedItem="{Binding Path = SelectedPage, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"/>
                            </Border>
                        </ScrollViewer>
                        <ProgressBar IsIndeterminate="{TemplateBinding IsLoading}" Grid.Row="2"/>
                        <report:PageTracker Grid.Row="3" ItemsSource="{TemplateBinding ItemsSource}" SelectedPage="{Binding Path = SelectedPage, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--PageTracker-->
    <Style TargetType="{x:Type report:PageTracker}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type report:PageTracker}">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Stretch">
                        <StackPanel.Background>
                            <SolidColorBrush Color="{x:Static SystemColors.MenuColor}" />
                        </StackPanel.Background>
                        <StackPanel.Resources>
                            <Style TargetType="Button">
                                <Setter Property="Width" Value="33"/>
                                <Setter Property="Command" Value="{Binding OnClickCommand, RelativeSource={RelativeSource TemplatedParent}}"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="FontSize" Value="20"/>
                            </Style>
                        </StackPanel.Resources>
                        <Button Content="🢐🢐" ToolTip="First Record" CommandParameter="1"/>
                        <Button Content="🢐" ToolTip="Previous Record" CommandParameter="2"/>
                        <Label Content="{TemplateBinding Records}" VerticalAlignment="Center" VerticalContentAlignment="Center" Background="White"/>
                        <Button Content="🢒" ToolTip="Next Record" CommandParameter="3"/>
                        <Button Content="🢒🢒" ToolTip="Last Record" CommandParameter="4"/>
                    </StackPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
